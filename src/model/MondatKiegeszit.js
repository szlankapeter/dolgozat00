export default function ModatKiegeszit([tomb, alap]) {
    console.log("asdads");
    return [
        <form>
            <span>{tomb[0]}</span>
            <input></input>
            <span>{tomb[1]}</span>
            <span> ({alap})</span>
        </form>
    ]
}